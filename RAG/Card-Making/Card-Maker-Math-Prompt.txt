You are creating Mentora Learning Cards from one Persian textbook page image.

Return exactly one valid JSON object.
Return JSON only.
Do not write markdown.
Do not write explanations.
Do not write anything before or after the JSON.
Do not use markdown formatting inside JSON string values.

Top-level output rule:
Return one JSON object with this shape:

{
  "cards": [
    {
      "card_id": "{MAIN_SUBJECT}_{GRADE}_{BOOK_CODE}_p{PAGE_NUMBER_3DIGITS}_c01",
      "main_subject": "{MAIN_SUBJECT}",
      "subsubject": "choose_only_from_allowed_list",
      "tag": "{MAIN_SUBJECT}/choose_only_from_allowed_list",
      "summary": "خلاصه کوتاه فارسی فقط بر اساس فرمول‌ها، آموزش‌ها و مثال‌های حل‌شده صفحه.",
      "key_points": [
        "فرمول، قاعده، روش یا نکته مهمی که دقیقا در صفحه دیده می‌شود."
      ],
      "main_text": "فقط فرمول‌ها، متن‌های آموزشی مهم، و مثال‌های حل‌شده خود کتاب.",
      "other_details": [
        "جزئیات مفید شکل، جدول، نمودار یا کپشن، اگر وجود دارد."
      ],
      "source_page": {PAGE_NUMBER},
      "image_path": "Books/{BOOK_CODE}/page_{PAGE_NUMBER_3DIGITS}.png",
      "confidence": 0.85
    }
  ]
}

If the page needs only one card, return exactly one item in "cards".
If the page clearly contains two or more separate useful educational topics, return multiple card objects in "cards".
If the page is unreadable, empty, or only contains ignored non-exam sections, return:

{
  "cards": [],
  "page_status": "no_extract_worthy_content",
  "reason": "No useful formula, teaching text, or solved example was found.",
  "source_page": {PAGE_NUMBER},
  "image_path": "Books/{BOOK_CODE}/page_{PAGE_NUMBER_3DIGITS}.png",
  "confidence": 0.85
}

If the page only contains unsolved exercises, unsolved tests, or unsolved activities, return:

{
  "cards": [],
  "page_status": "no_extract_worthy_content",
  "reason": "Only unsolved exercises/tests were found. No formula, teaching text, or solved example.",
  "source_page": {PAGE_NUMBER},
  "image_path": "Books/{BOOK_CODE}/page_{PAGE_NUMBER_3DIGITS}.png",
  "confidence": 0.9
}

Important exercise/test rule:
If the page title or main section is «تمرین» and the page only contains questions without the book's own solution, return the no_extract_worthy_content object above.
Formulas, function definitions, tables, and graphs that appear only as part of unsolved exercise/test questions are not extract-worthy content.
Do not create cards from those formulas or graphs.

Previous cards context:
{PREVIOUS_CARDS_CONTEXT}

Use the previous cards only to understand the continuity of the lesson, chapter, topic, and subsubject.
Do not copy previous card text into the current page cards.
The current output cards must be based on the current page image.

Very important rules:
- فقط و فقط از اطلاعاتی استفاده کن که در تصویر صفحه دیده می‌شود.
- هیچ نکته، فرمول، مثال، تعریف، تست، یا توضیحی از دانش خودت اضافه نکن.
- متن‌های اصلی کتاب را فارسی نگه دار.
- برای ریاضی، main_text نباید کپی کامل همه متن‌های صفحه باشد.
- main_text فقط باید شامل موارد مهم آموزشی زیر باشد: فرمول‌ها، قواعد، روش‌های حل، تعریف‌های ضروری، و مثال‌های حل‌شده توسط خود کتاب.
- اگر بخشی از متن واضح نیست، حدس نزن.
- تمرین‌ها را حل نکن مگر اینکه پاسخ یا توضیح آن در خود صفحه دیده شود.
- تمرین‌ها، سوال‌ها و فعالیت‌های حل‌نشده را وارد main_text نکن.
- از تمرین‌ها، سوال‌ها و فعالیت‌های حل‌نشده کارت جدا نساز.
- تمرین‌ها، سوال‌ها و فعالیت‌های حل‌نشده را در other_details هم وارد نکن، مگر اینکه خود کتاب جواب یا توضیح حل آن‌ها را در همان صفحه داده باشد.
- اگر فرمول، نمودار یا جدول فقط بخشی از صورت سوال تمرین/تست حل‌نشده است، آن را محتوای آموزشی حساب نکن و از آن کارت نساز.
- summary می‌تواند خلاصه‌سازی کند، اما فقط بر اساس فرمول‌ها، آموزش‌ها و مثال‌های حل‌شده دیده‌شده در صفحه.
- key_points فقط باید شامل فرمول‌ها، قواعد، روش‌ها یا نکته‌هایی باشد که در صفحه دیده می‌شوند.

Math main_text rules:
- اگر در صفحه فرمول، رابطه، اتحاد، تعریف نمادین یا قاعده ریاضی وجود دارد، آن را با بهترین قالب ممکن بنویس.
- برای فرمول‌ها از قالب خوانا استفاده کن.
- اگر LaTeX می‌نویسی، JSON باید معتبر بماند و هر بک‌اسلش LaTeX باید دو بک‌اسلش باشد؛ مثلا بنویس "\\frac{1}{k}" نه "\frac{1}{k}".
- اگر مطمئن نیستی LaTeX را درست escape می‌کنی، فرمول را با متن ساده و خوانا بنویس؛ مثلا بنویس "(1/k)".
- اگر صفحه یک روش، تابع، مفهوم، تعریف یا قاعده را آموزش می‌دهد، فقط همان متن آموزشی مهم را بنویس.
- متن‌های توضیحی اضافه، حاشیه‌ای، تکراری، یا جمله‌هایی که برای حل سوال لازم نیستند را وارد نکن.
- اگر در تصویر مثال حل‌شده وجود دارد و خود کتاب آن را حل کرده است، متن مثال و مراحل حل دیده‌شده را وارد کن.
- اگر یک مثال فقط صورت سوال دارد و حل نشده است، آن را وارد main_text نکن و خودت حل نکن.
- اگر جواب نهایی یا بخشی از حل در تصویر دیده نمی‌شود، آن بخش را حدس نزن.

Ignored sections:
این بخش‌ها مهم نیستند و نباید وارد کارت شوند:
- «بیشتر بدانیم»
- «خوب است بدانید»
- «تاریخ علم»
- «پانویس»
- هر بخش حاشیه‌ای یا اطلاعات اضافه‌ای که برای امتحان و یادگیری اصلی لازم نیست

این بخش‌ها را در main_text، key_points، summary و other_details وارد نکن.
اگر فقط عنوان این بخش‌ها دیده می‌شود، آن‌ها را نادیده بگیر.
اگر کل صفحه تقریبا فقط از این بخش‌های غیرضروری تشکیل شده، مقدار "cards" را آرایه خالی برگردان.

Multi-card decision rules:
- اگر صفحه فقط یک مفهوم اصلی دارد، فقط یک کارت بساز.
- اگر صفحه چند بخش آموزشی مستقل دارد، برای هر بخش مهم و مستقل یک کارت جدا بساز.
- اگر یک صفحه شامل دو زیرموضوع متفاوت از لیست مجاز است، برای هر زیرموضوع یک کارت جدا بساز.
- اگر مثال حل‌شده، شکل، جدول یا نمودار فقط برای توضیح همان مفهوم اصلی است، کارت جدا نساز؛ آن را در همان کارت بیاور.
- اگر مثال یا فعالیت آموزشی خودش یک روش یا مفهوم مستقل و مهم دارد، می‌توانی برای آن کارت جدا بسازی.
- بی‌دلیل کارت‌های زیاد نساز. کیفیت و تفکیک درست مهم‌تر از تعداد کارت است.
- برای فعالیت‌ها، تمرین‌ها و سوال‌های حل‌نشده کارت نساز.
- هیچ کارتی برای بخش‌های «بیشتر بدانیم»، «خوب است بدانید»، «تاریخ علم» یا «پانویس» نساز.

Card numbering:
- card_id هر کارت باید با شماره کارت متفاوت تمام شود.
- اولین کارت صفحه باید c01 باشد.
- دومین کارت صفحه باید c02 باشد.
- سومین کارت صفحه باید c03 باشد.
- برای هر صفحه، شماره کارت از c01 شروع می‌شود.

Subsubject rule:
You must choose subsubject only from this allowed list:
{ALLOWED_SUBSUBJECTS}

Do not invent a new subsubject.
Do not translate or rename the allowed subsubjects.
If a useful page does not clearly match any allowed subsubject, use:
"subsubject": "general"

For each card:
- tag must be "{MAIN_SUBJECT}/{subsubject}"
- main_subject must be exactly "{MAIN_SUBJECT}"
- source_page must be exactly {PAGE_NUMBER}
- image_path must be exactly "Books/{BOOK_CODE}/page_{PAGE_NUMBER_3DIGITS}.png"

Metadata:
main_subject = "{MAIN_SUBJECT}"
grade = "{GRADE}"
book_code = "{BOOK_CODE}"
page_number = "{PAGE_NUMBER}"
page_number_3digits = "{PAGE_NUMBER_3DIGITS}"

Field rules:
- summary: خلاصه کوتاه ۱ تا ۳ جمله‌ای، فقط بر اساس محتوای آموزشی مهم همان کارت.
- key_points: فقط تعریف‌ها، قواعد، فرمول‌ها، روش‌ها و نکته‌های مهمی که در صفحه دیده می‌شوند.
- main_text: فقط این موارد را بنویس: ۱) فرمول‌ها و روابط مهم، ۲) متن آموزشی مهم درباره روش/تابع/مفهوم/قاعده، ۳) مثال‌هایی که خود کتاب حل کرده است همراه با مراحل حل دیده‌شده.
- other_details: فقط جزئیات مفید مثل شکل، جدول، نمودار، کپشن مهم، یا توضیح تصویری مربوط به همان کارت.
- داخل هیچ فیلدی از markdown مثل ###، **bold**، bullet markdown یا code fence استفاده نکن.
- confidence: عددی بین 0 و 1.

Confidence guide:
- 0.90 تا 1.00: صفحه واضح است و استخراج بسیار قابل اعتماد است.
- 0.70 تا 0.89: بیشتر صفحه واضح است، اما کمی عدم قطعیت وجود دارد.
- 0.40 تا 0.69: بخشی از صفحه نامشخص است یا انتخاب زیرموضوع کمی نامطمئن است.
- 0.00 تا 0.39: صفحه ناخوانا، کم‌فایده، یا تقریبا خالی است.

Return JSON only.

